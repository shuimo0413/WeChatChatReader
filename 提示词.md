
# 提示词
# 本文档是给AI读的

你是本仓库的协作助手。工作目录默认为项目根（含 `pyproject.toml`、`data.yaml`）。包管理用 **uv**，执行 Python 用 **`uv run python ...`**。

---

## 项目是做什么的

Windows 微信聊天区 **截图 → 标注 → YOLO 训练/推理 → OCR 导出 JSON**。检测类（`data.yaml`）：`0=chat_bubble`，`1=chat_object`，`2=distraction`。气泡左右角色由 **`bubble_role.py`** 的 HSV 绿占比粗分 `self`/`peer`，与 **`predict_bubbles.py`** 的 `--min-green-ratio`、`--shrink-ratio` 相关。

---

## 数据流（顺序）

`before_img/`（截图）→ `img/`+`labels/`（标注）→ **`split_dataset`** → `dataset/` → **`train_bubbles`** → `runs/detect/<run>/weights/best.pt` → **`predict_bubbles`** → `predict_out/`（`*_vis.jpg`、`*.json`、`summary.json`）→ **`text_process`** → `text_json/`、`text_img/`。

网页标注读 **`before_img`**，写 **`img`/`labels`**；半自动 **`label_review`** 默认读 **`before_img`**、写 **`labels`**，并可镜像到 **`img`**（供 split 用）。

---

## 代码里的默认权重（与 README 示例可能不一致）

- **predict_bubbles.py**、**tools/label_review.py** 源码默认：`runs/detect/wechat_bubbles-3/weights/best.pt`
- **`README.MD`** 执行步骤示例仍可能写 `wechat_bubbles-7`：改参数或改文档时以用户指定及 **`runs/detect/` 下实际存在的 `best.pt`** 为准。

---

## 常用命令（均在项目根执行）

```bash
uv sync
```

```bash
uv run python tools/wechat_window_loop_ocr.py --interval-ms 1000
```

```bash
uv run python tools/fount/server.py
```

```bash
uv run python tools/label_review.py --weights runs/detect/wechat_bubbles-3/weights/best.pt
```

```bash
uv run python tools/split_dataset.py --train-ratio 0.8
```

```bash
uv run python train_bubbles.py
```

```bash
uv run python train_bubbles.py --model runs/detect/wechat_bubbles-3/weights/best.pt
```

```bash
uv run python predict_bubbles.py --source test
```

```bash
uv run python predict_bubbles.py --weights runs/detect/wechat_bubbles-3/weights/best.pt --source before_img
```

```bash
uv run python text_process.py
```

```bash
uv run python text_process.py --ocr-device cpu
```

---

## 常用提示词与对应命令

| 用户说法 | AI 应执行 / 应改哪里 |
|----------|----------------------|
| **请将模型换成 `xxx`** | `xxx` 为 run 名（如 `wechat_bubbles-5`）时，权重路径为 `runs/detect/<xxx>/weights/best.pt`。改代码：`predict_bubbles.py` 里 `--weights` 的 `default=`；`tools/label_review.py` 里 `_DEFAULT_WEIGHTS_PARTS`（元组里 detect 下那一级目录名）。`xxx` 若是绝对/相对 `.pt` 路径则按路径写死。改前用工具确认 `best.pt` 存在。临时不改代码：`uv run python predict_bubbles.py --weights <路径> --source ...`、`uv run python tools/label_review.py --weights <路径>`。README/提示词里示例路径可一并替换。 |
| **请修改截图频率为 `xxx`** | `xxx` 为毫秒。运行：`uv run python tools/wechat_window_loop_ocr.py --interval-ms <xxx>`。若用户要默认频率永久生效：改 `tools/wechat_window_loop_ocr.py` 里 `add_argument("--interval-ms", ... default=...)`。 |
| **请帮我安装环境** | 项目根执行：`uv sync`。需要跑 `text_process.py` 时再装 OCR：`uv pip install rapidocr-onnxruntime` 以及 `onnxruntime` 或 `onnxruntime-gpu`（与 `--ocr-device cpu/gpu` 一致）。 |
| **对 `某目录` 跑一遍检测 / 推理** | `uv run python predict_bubbles.py --source <目录或单图>`；指定权重 `--weights .../best.pt`；输出目录 `--output <dir>`（默认 `predict_out/`）。 |
| **把检测结果弄成聊天 JSON（OCR）** | 先有 `predict_out/` 里每张图对应 `*.json`，再 `uv run python text_process.py`；JSON 不在默认处：`--predict-out <dir>`，输出 `--text-json` / `--text-img`。 |
| **OCR 用 CPU / 用 GPU** | `uv run python text_process.py --ocr-device cpu` 或 `--ocr-device gpu`（需对应 onnxruntime 包装好）。 |
| **不要保存气泡裁图，只要文字 JSON** | `uv run python text_process.py --no-save-crop`。 |
| **重新划分训练集和验证集（比例 xxx）** | `uv run python tools/split_dataset.py --train-ratio <0~1>`；换划分随机性改 `--seed`；写入 `dataset/`，之后重新训练。 |
| **从上次 best 继续训练 / 微调** | `uv run python train_bubbles.py --model runs/detect/<run名>/weights/best.pt`；可调 `--epochs`、`--batch`、`--name` 新 run 名。 |
| **训练完要看验证集带框预览图** | `uv run python train_bubbles.py --save-val-vis`（其余训练参数照常）；若 run 目录被 ultralytics 改名，对照 `runs/detect/` 实际文件夹找 `best.pt`。 |
| **检测框漏得多 / 误检多** | `predict_bubbles.py`、`label_review.py`：调 `--conf`（漏检略降、误检略升，默认 0.25）。 |
| **半截气泡还在 / 正常气泡被当成半截滤掉** | `predict_bubbles.py`：`--edge-overlap-threshold`（滤不干净则降，误杀则升，默认 0.30）。 |
| **自己/对方（绿泡）判反或不稳** | `predict_bubbles.py`：`--min-green-ratio`、`--shrink-ratio`；仍不对再改 `bubble_role.py` 里 HSV 默认区间。 |
| **打开网页标注** | `uv run python tools/fount/server.py`，用浏览器打开终端打印的地址（常见 `http://127.0.0.1:8810/`）。 |
| **半自动标注换一批图（目录 xxx）** | `uv run python tools/label_review.py --images <目录>`；标签仍默认 `labels/`，可加 `--weights`、`--no-img-mirror`。 |
| **截图脚本找不到微信 / 多开进程** | `tools/wechat_window_loop_ocr.py`：`--exe 某.exe` 可重复指定；`--interval-ms` 控制间隔。 |
| **训练太占显存 / 太慢** | `train_bubbles.py`：减 `--batch`、开 `--half`、试 `--cache disk`、`--no-plots`；Windows 保持 `--workers 0` 一般更稳。 |

```bash
uv sync
uv pip install rapidocr-onnxruntime onnxruntime
```

---

## 操作本项目时优先打开的文件

| 目的 | 路径 |
|------|------|
| 类别与数据路径 | `data.yaml` |
| 训练入口 | `train_bubbles.py` |
| 推理与 JSON 结构 | `predict_bubbles.py` |
| OCR 与 speaker 兜底 | `text_process.py`、`bubble_role.py` |
| 半自动标 | `tools/label_review.py` |
| 划分集 | `tools/split_dataset.py` |
| 人机可读流程与参数表 | `README.MD` |

---

## 改参数/改代码时注意

- 改 **`data.yaml` 的 `nc`/`names` 顺序** 须与标注 **`labels/*.txt` 类别 id** 及推理 **`--bubble-class`** 一致。
- **`split_dataset`** 只复制「`img/` 与 `labels/` 同名 stem 且均有文件」的样本。
- **`text_process`** 依赖 RapidOCR；缺包时先装 `rapidocr-onnxruntime` 与 `onnxruntime` 或 `onnxruntime-gpu`。
- Windows 训练 **`train_bubbles.py --workers`** 默认 0 较稳。

---

## 用户可能下的指令（简写对应）

- 「截图」→ `wechat_window_loop_ocr.py`
- 「网页标」→ `fount/server.py`
- 「半自动标」→ `label_review.py`
- 「划分」→ `split_dataset.py`
- 「训练 / 微调」→ `train_bubbles.py`（微调加 `--model .../best.pt`）
- 「推理」→ `predict_bubbles.py`（`--source` 指向图片目录或单图）
- 「出聊天 JSON」→ `text_process.py`
- 「调 conf / 调阈值」→ `predict_bubbles.py` 或 `label_review.py` 的 `--conf`；半截气泡 → `--edge-overlap-threshold`；绿泡角色 → `--min-green-ratio` / `--shrink-ratio`
- 「改数据集比例」→ `split_dataset.py --train-ratio`
- 「只要 JSON 不要裁图」→ `text_process.py --no-save-crop`
- 「OCR 走 CPU」→ `text_process.py --ocr-device cpu`

更细的参数含义与调参方向见 **`README.MD` → 补充说明**。
